// Child class definition, which overrides the show() method
class Child extends Mother {
    // Overriding the show() method in Child class
    @Override
    void show() {
        System.out.println("Hello JUET");
    }
}